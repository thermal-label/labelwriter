[**labelwriter**](../../../README.md)

***

[labelwriter](../../../README.md) / [core/src](../README.md) / build550Mode

# Function: build550Mode()

> **build550Mode**(`mode`): `Uint8Array`

`ESC h` (text) / `ESC i` (graphics) — Select Output Mode.

## Parameters

### mode

`"text"` \| `"graphics"`

## Returns

`Uint8Array`
