# Generated by typedoc-plugin-markdown via `pnpm docs:api`.
# typedoc owns the format; prettier reformatting it conflicts with the
# pre-push hook that diffs typedoc's output against the working tree.
docs/api/

# Generated by scripts/compile-data.mjs from data/devices/*.json5 and
# data/media.json5. The script owns the layout; let it re-emit on each
# typecheck/test run without prettier-roundtripping the result.
packages/core/data/devices.json
packages/core/data/media.json
packages/core/src/*.generated.ts
